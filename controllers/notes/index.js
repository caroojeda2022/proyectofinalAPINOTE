const newNote = require('./newNote');
const listNote = require('./listNote');
const getNote = require('./getNote');
const votePublicNote = require('./votePublicNote');
const deleteNote = require('./deleteNote');

module.exports = {
  newNote,
  listNote,
  getNote,
  votePublicNote,
  deleteNote,
};

